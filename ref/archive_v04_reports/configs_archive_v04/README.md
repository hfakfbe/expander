# Configs Archive v0.4

This directory stores configuration files from the pre-v0.5 experiment flow.

The active v0.5 copy experiment keeps only these configs in the root `configs/`
directory:

- `configs/copy_v05_smoke.json`
- `configs/copy_v05_main.json`

Archived configs here are retained for historical reference and should not be
mixed into v0.5 result tables.
