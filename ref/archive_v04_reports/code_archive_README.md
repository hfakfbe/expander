# Code Archive

The old external `code/lra-benchmarks/` checkout was moved to:

```text
ref/archive_v04_reports/code_archive/lra-benchmarks/
```

That archive is ignored by git because it is an external checkout and data
snapshot, not part of the v0.5 copy-only code path.
