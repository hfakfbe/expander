# Results Archive v0.4

Previous experiment artifacts were moved to:

```text
ref/archive_v04_reports/results_archive_v04/
```

The archive contains old `outputs/`, `logs/`, and `cached_graphs/` artifacts from
the pre-v0.5 experiment flow. It is ignored by git because these are generated
experiment artifacts.

The active v0.5 copy experiment artifacts remain in the root `outputs/` and
`logs/` directories under `copy_v05_*` names.
