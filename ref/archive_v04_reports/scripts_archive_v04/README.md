# Scripts Archive v0.4

This directory stores scripts from the pre-v0.5 experiment flow.

The active v0.5 copy experiment keeps only these runnable scripts in the root
`scripts/` directory:

- `scripts/graph_structures.py`
- `scripts/synthetic_mvp.py`

Archived scripts here are retained for historical reference and should not be
used as v0.5 result sources.
