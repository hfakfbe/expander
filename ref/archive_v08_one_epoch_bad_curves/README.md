# v08 one-epoch / bad-curves archive

This archive preserves the superseded v08 run that used one train-equivalent epoch and produced invalid 1x1 `training_curves.png` files when matplotlib was missing on the remote host.

These artifacts are provenance only and must not be used as current Phase 5/6 results. The active v08 rerun uses `resolved_train_equivalent_epochs=5` and audits `training_curves.png` dimensions/size in each `result_field_audit.json`.
